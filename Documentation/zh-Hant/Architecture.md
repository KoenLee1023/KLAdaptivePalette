# 架構

分析器會先依照 `KLPaletteAnalysisConfiguration` 的尺寸和像素數上限產生光柵，再掃描像素。一般情況會保留原始影像的長寬比。對於無法讓兩個維度都至少保留一個像素的極端比例影像，像素數上限優先。分析時排除 alpha 小於或等於 0.5 的像素，以感知直方圖排序可見色彩，並根據實測亮度選取對比色。平坦邊緣保留代表光柵色。帶有紋理的中性色或彩色邊緣會從同一色彩範圍中選取合適的光柵色。
