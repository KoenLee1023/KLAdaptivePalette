# Getting Started

Create `KLPaletteRequest(image:cacheID:containerSize:)`, preserving the host's content mode and crop anchor, then call `KLPaletteAnalyzer().analyze(_)`. Use the returned foreground paired with its actual background.
