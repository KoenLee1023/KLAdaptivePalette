# 架构

分析器会先按照 `KLPaletteAnalysisConfiguration` 的尺寸和像素数上限生成栅格，再扫描像素。通常会保留源图宽高比。对于无法在两个维度都至少保留一个像素的极端比例图像，像素数上限优先。分析时忽略 alpha 小于或等于 0.5 的像素，通过感知直方图排序可见颜色，并根据实测亮度选择对比色。平坦边缘保留代表栅格色。带纹理的中性或彩色边缘会从同一颜色范围内选择合适的栅格色。
